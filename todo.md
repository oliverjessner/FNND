# AI Fix Todo

This file was generated by ItWorksBut. It is optimized for coding agents: work top to bottom, keep changes focused, and mark tasks complete only after verification.

## Agent Rules

- Fix critical and high findings before lower-priority cleanup.
- Inspect the referenced code before editing, especially for heuristic findings.
- Preserve existing behavior unless the finding explicitly requires changing it.
- Add or update focused tests when the change has behavioral risk.
- Do not silence ItWorksBut checks unless the underlying issue is actually fixed.
- Never print, log, or preserve raw secret values. Use placeholders only.

## Scan Summary

- Tool: ItWorksBut 0.7.3
- Project: /Users/oli/github/NO-BULLSHIT-RSS
- Completed: 2026-09-01T15:07:09.373Z
- Files scanned: 596
- Text files scanned: 295
- Fail-on: low
- Exit decision: 1
- Total findings: 8
- Critical: 0
- High: 8
- Medium: 0
- Low: 0
- Info: 0

## High Findings

### 1. It works, but this ID lookup may belong to someone else.

- [x] Fix this finding.
- Check ID: `api.idor-risk`
- Severity: `high`
- Category: `auth`
- Location: `backend/services/digest-store.test.js:120`
- Heuristic: `yes`
- Tags: api, auth, idor, heuristic
- Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check.
- Recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.idor-risk at backend/services/digest-store.test.js:120. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check. Required change: Scope object access by authenticated user, owner, tenant, account, or organization in addition to object id. Existing recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [x] The issue behind `api.idor-risk` is fixed at the source.
- [x] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [x] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [x] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 2. It works, but this ID lookup may belong to someone else.

- [x] Fix this finding.
- Check ID: `api.idor-risk`
- Severity: `high`
- Category: `auth`
- Location: `backend/services/digest-store.test.js:126`
- Heuristic: `yes`
- Tags: api, auth, idor, heuristic
- Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check.
- Recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.idor-risk at backend/services/digest-store.test.js:126. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check. Required change: Scope object access by authenticated user, owner, tenant, account, or organization in addition to object id. Existing recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [x] The issue behind `api.idor-risk` is fixed at the source.
- [x] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [x] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [x] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 3. It works, but your API believes whatever the request says.

- [x] Fix this finding.
- Check ID: `api.no-schema-validation`
- Severity: `high`
- Category: `api`
- Location: `backend/routes/articles.js:183`
- Heuristic: `yes`
- Tags: api, validation, heuristic
- Problem: This API route appears to consume request input without an obvious schema validation step.
- Recommendation: Validate request body, query and params with a schema library such as Zod, Joi, Valibot, AJV or equivalent.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.no-schema-validation at backend/routes/articles.js:183. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: This API route appears to consume request input without an obvious schema validation step. Required change: Validate request body, query and params with a schema library such as Zod, Joi, Valibot, AJV or equivalent. Existing recommendation: Validate request body, query and params with a schema library such as Zod, Joi, Valibot, AJV or equivalent. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [x] The issue behind `api.no-schema-validation` is fixed at the source.
- [x] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [x] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [x] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 4. It works, but your API believes whatever the request says.

- [x] Fix this finding.
- Check ID: `api.no-schema-validation`
- Severity: `high`
- Category: `api`
- Location: `backend/routes/feeds.js:102`
- Heuristic: `yes`
- Tags: api, validation, heuristic
- Problem: This API route appears to consume request input without an obvious schema validation step.
- Recommendation: Validate request body, query and params with a schema library such as Zod, Joi, Valibot, AJV or equivalent.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.no-schema-validation at backend/routes/feeds.js:102. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: This API route appears to consume request input without an obvious schema validation step. Required change: Validate request body, query and params with a schema library such as Zod, Joi, Valibot, AJV or equivalent. Existing recommendation: Validate request body, query and params with a schema library such as Zod, Joi, Valibot, AJV or equivalent. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [x] The issue behind `api.no-schema-validation` is fixed at the source.
- [x] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [x] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [x] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 5. It works, but your API believes whatever the request says.

- [x] Fix this finding.
- Check ID: `api.no-schema-validation`
- Severity: `high`
- Category: `api`
- Location: `backend/routes/lists.js:68`
- Heuristic: `yes`
- Tags: api, validation, heuristic
- Problem: This API route appears to consume request input without an obvious schema validation step.
- Recommendation: Validate request body, query and params with a schema library such as Zod, Joi, Valibot, AJV or equivalent.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.no-schema-validation at backend/routes/lists.js:68. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: This API route appears to consume request input without an obvious schema validation step. Required change: Validate request body, query and params with a schema library such as Zod, Joi, Valibot, AJV or equivalent. Existing recommendation: Validate request body, query and params with a schema library such as Zod, Joi, Valibot, AJV or equivalent. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [x] The issue behind `api.no-schema-validation` is fixed at the source.
- [x] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [x] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [x] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 6. It works, but your SQL query is one template string away from pain.

- [x] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `backend/services/topics.js:420`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at backend/services/topics.js:420. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [x] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [x] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [x] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [x] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 7. It works, but your SQL query is one template string away from pain.

- [x] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `cli/cli.test.js:52`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at cli/cli.test.js:52. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [x] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [x] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [x] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [x] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 8. It works, but your SQL query is one template string away from pain.

- [x] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `public/js/views/settings.js:212`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at public/js/views/settings.js:212. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [x] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [x] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [x] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [x] The relevant scanner finding no longer appears after rerunning ItWorksBut.


## Final Verification

- [x] Run the relevant test suite.
- [x] Run `itworksbut scan` again.
- [x] Confirm there are no remaining findings at or above the configured fail-on severity.

