# AI Fix Todo

This file was generated by ItWorksBut. It is optimized for coding agents: work top to bottom, keep changes focused, and mark tasks complete only after verification.

## Agent Rules

- Fix critical and high findings before lower-priority cleanup.
- Inspect the referenced code before editing, especially for heuristic findings.
- Preserve existing behavior unless the finding explicitly requires changing it.
- Add or update focused tests when the change has behavioral risk.
- Do not silence ItWorksBut checks unless the underlying issue is actually fixed.
- Never print, log, or preserve raw secret values. Use placeholders only.

## Scan Summary

- Tool: ItWorksBut 0.3.0
- Project: /Users/oli/github/NO-BULLSHIT-RSS
- Completed: 2026-05-11T08:12:54.207Z
- Files scanned: 78
- Text files scanned: 36
- Fail-on: low
- Exit decision: 1
- Total findings: 83
- Critical: 0
- High: 77
- Medium: 6
- Low: 0
- Info: 0

## High Findings

### 1. It works, but this ID lookup may belong to someone else.

- [ ] Fix this finding.
- Check ID: `api.idor-risk`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/articles.js:284`
- Heuristic: `yes`
- Tags: api, auth, idor, heuristic
- Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check.
- Recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.idor-risk at backend/routes/articles.js:284. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check. Required change: Scope object access by authenticated user, owner, tenant, account, or organization in addition to object id. Existing recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.idor-risk` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 2. It works, but this ID lookup may belong to someone else.

- [ ] Fix this finding.
- Check ID: `api.idor-risk`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/feeds.js:114`
- Heuristic: `yes`
- Tags: api, auth, idor, heuristic
- Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check.
- Recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.idor-risk at backend/routes/feeds.js:114. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check. Required change: Scope object access by authenticated user, owner, tenant, account, or organization in addition to object id. Existing recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.idor-risk` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 3. It works, but this ID lookup may belong to someone else.

- [ ] Fix this finding.
- Check ID: `api.idor-risk`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/feeds.js:64`
- Heuristic: `yes`
- Tags: api, auth, idor, heuristic
- Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check.
- Recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.idor-risk at backend/routes/feeds.js:64. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check. Required change: Scope object access by authenticated user, owner, tenant, account, or organization in addition to object id. Existing recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.idor-risk` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 4. It works, but this ID lookup may belong to someone else.

- [ ] Fix this finding.
- Check ID: `api.idor-risk`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/feeds.js:77`
- Heuristic: `yes`
- Tags: api, auth, idor, heuristic
- Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check.
- Recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.idor-risk at backend/routes/feeds.js:77. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check. Required change: Scope object access by authenticated user, owner, tenant, account, or organization in addition to object id. Existing recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.idor-risk` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 5. It works, but this ID lookup may belong to someone else.

- [ ] Fix this finding.
- Check ID: `api.idor-risk`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/lists.js:38`
- Heuristic: `yes`
- Tags: api, auth, idor, heuristic
- Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check.
- Recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.idor-risk at backend/routes/lists.js:38. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check. Required change: Scope object access by authenticated user, owner, tenant, account, or organization in addition to object id. Existing recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.idor-risk` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 6. It works, but this ID lookup may belong to someone else.

- [ ] Fix this finding.
- Check ID: `api.idor-risk`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/lists.js:62`
- Heuristic: `yes`
- Tags: api, auth, idor, heuristic
- Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check.
- Recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.idor-risk at backend/routes/lists.js:62. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check. Required change: Scope object access by authenticated user, owner, tenant, account, or organization in addition to object id. Existing recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.idor-risk` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 7. It works, but this ID lookup may belong to someone else.

- [ ] Fix this finding.
- Check ID: `api.idor-risk`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/webhook.js:130`
- Heuristic: `yes`
- Tags: api, auth, idor, heuristic
- Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check.
- Recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.idor-risk at backend/routes/webhook.js:130. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Potential IDOR risk: SQL lookup by id appears without a nearby owner, tenant, or user scope check. Required change: Scope object access by authenticated user, owner, tenant, account, or organization in addition to object id. Existing recommendation: Scope object reads and writes by authenticated user, owner, account, tenant, or organization, not by id alone. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.idor-risk` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 8. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/articles.js:314`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /stats appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/articles.js:314. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /stats appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 9. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/articles.js:319`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /digest appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/articles.js:319. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /digest appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 10. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/articles.js:320`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /daily-digest appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/articles.js:320. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /daily-digest appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 11. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/articles.js:324`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:id/digested appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/articles.js:324. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:id/digested appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 12. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/articles.js:325`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:id/daily-digested appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/articles.js:325. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:id/daily-digested appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 13. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/articles.js:326`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /digest/mark-all-digested appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/articles.js:326. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /digest/mark-all-digested appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 14. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/articles.js:327`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /daily-digest/mark-all-digested appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/articles.js:327. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /daily-digest/mark-all-digested appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 15. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/articles.js:329`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/articles.js:329. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 16. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/articles.js:382`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /lists/bulk appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/articles.js:382. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /lists/bulk appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 17. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/articles.js:389`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:id/lists appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/articles.js:389. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:id/lists appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 18. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/digest-settings.js:123`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /blocked-words/:id appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/digest-settings.js:123. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /blocked-words/:id appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 19. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/digest-settings.js:46`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/digest-settings.js:46. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 20. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/digest-settings.js:50`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /excluded-feeds appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/digest-settings.js:50. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /excluded-feeds appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 21. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/digest-settings.js:88`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /blocked-words appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/digest-settings.js:88. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /blocked-words appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 22. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/feeds.js:123`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:id appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/feeds.js:123. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:id appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 23. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/feeds.js:134`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /test/url appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/feeds.js:134. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /test/url appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 24. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/feeds.js:26`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/feeds.js:26. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 25. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/feeds.js:38`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/feeds.js:38. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 26. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/feeds.js:73`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:id appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/feeds.js:73. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:id appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 27. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/lists.js:136`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:id/items/bulk-delete appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/lists.js:136. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:id/items/bulk-delete appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 28. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/lists.js:176`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:id/items/:articleId appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/lists.js:176. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:id/items/:articleId appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 29. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/lists.js:22`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/lists.js:22. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 30. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/lists.js:27`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/lists.js:27. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 31. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/lists.js:44`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:id appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/lists.js:44. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:id appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 32. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/lists.js:67`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:id appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/lists.js:67. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:id appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 33. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/lists.js:78`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:id/items appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/lists.js:78. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:id/items appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 34. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/lists.js:93`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:id/items/bulk appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/lists.js:93. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:id/items/bulk appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 35. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/topics.js:103`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:slug appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/topics.js:103. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:slug appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 36. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/topics.js:130`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /:slug appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/topics.js:130. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /:slug appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 37. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/topics.js:149`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /reprocess appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/topics.js:149. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /reprocess appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 38. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/topics.js:166`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /article/:id appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/topics.js:166. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /article/:id appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 39. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/topics.js:176`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /classify-preview appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/topics.js:176. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /classify-preview appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 40. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/topics.js:45`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/topics.js:45. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 41. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/topics.js:54`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /rules appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/topics.js:54. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /rules appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 42. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/topics.js:58`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /validate appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/topics.js:58. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /validate appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 43. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/topics.js:73`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /rules appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/topics.js:73. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /rules appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 44. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/topics.js:93`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/topics.js:93. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route / appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 45. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/routes/webhook.js:105`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /articles appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/routes/webhook.js:105. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /articles appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 46. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/server.js:111`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /api/health appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/server.js:111. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /api/health appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 47. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/server.js:112`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /api/fetch/status appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/server.js:112. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /api/fetch/status appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 48. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/server.js:113`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /api/events appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/server.js:113. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /api/events appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 49. It works, but this API route appears to trust strangers.

- [ ] Fix this finding.
- Check ID: `api.missing-auth-on-routes`
- Severity: `high`
- Category: `auth`
- Location: `backend/server.js:115`
- Heuristic: `yes`
- Tags: api, auth, heuristic
- Problem: Possible unauthenticated API route /api/fetch/run appears without nearby auth middleware or checks.
- Recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding api.missing-auth-on-routes at backend/server.js:115. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible unauthenticated API route /api/fetch/run appears without nearby auth middleware or checks. Required change: Add explicit authentication and authorization to the route, or document why the route is intentionally public. Existing recommendation: Add explicit authentication middleware/checks near the route, or document why the route is intentionally public. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `api.missing-auth-on-routes` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 50. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `backend/routes/articles.js:102`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at backend/routes/articles.js:102. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 51. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `backend/routes/articles.js:188`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at backend/routes/articles.js:188. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 52. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `backend/routes/articles.js:356`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at backend/routes/articles.js:356. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 53. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `backend/routes/articles.js:53`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at backend/routes/articles.js:53. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 54. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `backend/routes/digest-settings.js:56`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at backend/routes/digest-settings.js:56. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 55. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `backend/services/topics.js:285`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at backend/services/topics.js:285. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 56. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `backend/services/topics.js:314`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at backend/services/topics.js:314. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 57. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `electron/main.cjs:108`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at electron/main.cjs:108. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 58. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `electron/main.cjs:109`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at electron/main.cjs:109. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 59. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `public/app.js:1109`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at public/app.js:1109. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 60. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `public/app.js:1267`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at public/app.js:1267. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 61. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `public/app.js:1349`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at public/app.js:1349. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 62. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `public/app.js:1398`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at public/app.js:1398. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 63. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:1058:28`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:1058. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 64. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:1313:27`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:1313. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 65. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:1364:27`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:1364. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 66. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:1429:30`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:1429. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 67. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:1469:37`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:1469. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 68. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:1555:30`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:1555. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 69. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:1567:28`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:1567. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 70. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:1582:29`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:1582. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 71. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:2050:30`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:2050. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 72. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:2205:20`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:2205. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 73. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:2239:22`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:2239. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 74. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:2296:24`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:2296. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 75. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:2521:24`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:2521. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 76. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:827:34`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:827. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 77. It works, but your frontend is injecting HTML with sharp edges.

- [ ] Fix this finding.
- Check ID: `web.dangerous-inner-html`
- Severity: `high`
- Category: `web`
- Location: `public/app.js:865:39`
- Heuristic: `yes`
- Tags: web, xss, frontend
- Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled.
- Recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding web.dangerous-inner-html at public/app.js:865. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: innerHTML assignment appears to be used. This can create XSS risk if any input is attacker-controlled. Required change: Remove direct HTML injection or add proven sanitization at the trust boundary before rendering. Existing recommendation: Avoid raw HTML insertion when possible. If HTML is required, sanitize with a proven sanitizer and keep trusted and untrusted content separate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `web.dangerous-inner-html` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

## Medium Findings

### 1. It works, but your database schema has no paper trail.

- [ ] Fix this finding.
- Check ID: `database.no-migrations`
- Severity: `medium`
- Category: `database`
- Location: `project-wide`
- Heuristic: `yes`
- Tags: database, deployment
- Problem: Database or ORM usage appears to exist, but no migrations directory was found.
- Recommendation: Add versioned database migrations and run them through a controlled deployment process.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.no-migrations at the affected project files. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Database or ORM usage appears to exist, but no migrations directory was found. Required change: Add versioned database migrations that match the detected ORM or database stack. Existing recommendation: Add versioned database migrations and run them through a controlled deployment process. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.no-migrations` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 2. It works, but install lifecycle scripts should be reviewed.

- [ ] Fix this finding.
- Check ID: `dependencies.install-scripts-risk`
- Severity: `medium`
- Category: `dependencies`
- Location: `package.json`
- Heuristic: `no`
- Tags: dependencies, supply-chain, npm
- Problem: package.json defines an npm postinstall lifecycle script. This can run during dependency installation.
- Recommendation: Review whether the install-time script is necessary. In CI, prefer npm ci and consider --ignore-scripts where appropriate.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding dependencies.install-scripts-risk at package.json. Treat this as a concrete finding. Problem: package.json defines an npm postinstall lifecycle script. This can run during dependency installation. Required change: Review install lifecycle scripts, remove them if unnecessary, or document and constrain them so CI installs stay predictable. Existing recommendation: Review whether the install-time script is necessary. In CI, prefer npm ci and consider --ignore-scripts where appropriate. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `dependencies.install-scripts-risk` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 3. It works, but this pattern may be risky: .env.example should document required environment variables.

- [ ] Fix this finding.
- Check ID: `env.env-example-missing`
- Severity: `medium`
- Category: `env`
- Location: `project-wide`
- Heuristic: `yes`
- Tags: env, developer-experience, deployment
- Problem: Environment variable usage appears to exist, but .env.example is missing.
- Recommendation: Add .env.example with variable names and safe placeholder values so CI and contributors know what must be configured.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding env.env-example-missing at the affected project files. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Environment variable usage appears to exist, but .env.example is missing. Required change: Add .env.example with variable names and safe placeholder values so CI and contributors know what must be configured. Existing recommendation: Add .env.example with variable names and safe placeholder values so CI and contributors know what must be configured. Do not print, log, or preserve raw secret values; use placeholders only. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `env.env-example-missing` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 4. It works, but your .gitignore has holes.

- [ ] Fix this finding.
- Check ID: `git.gitignore-incomplete`
- Severity: `medium`
- Category: `git`
- Location: `.gitignore`
- Heuristic: `no`
- Tags: git, repo-hygiene, secrets
- Problem: .gitignore appears to be missing common entries: .env, .env.*, build/, .next/, coverage/, *.log, *.sqlite, *.db.
- Recommendation: Add the missing ignore patterns so local secrets, dependencies, logs, databases, and build output are not committed.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding git.gitignore-incomplete at .gitignore. Treat this as a concrete finding. Problem: .gitignore appears to be missing common entries: .env, .env.*, build/, .next/, coverage/, *.log, *.sqlite, *.db. Required change: Update .gitignore with the missing high-risk patterns without removing existing project-specific ignores. Existing recommendation: Add the missing ignore patterns so local secrets, dependencies, logs, databases, and build output are not committed. Do not print, log, or preserve raw secret values; use placeholders only. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `git.gitignore-incomplete` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 5. It works, but your HTTP headers are underdressed.

- [ ] Fix this finding.
- Check ID: `node.helmet-missing`
- Severity: `medium`
- Category: `node`
- Location: `project-wide`
- Heuristic: `no`
- Tags: node, express, headers
- Problem: Express appears to be used, but Helmet was not detected.
- Recommendation: Install and use helmet() early in the middleware stack, or document equivalent security header handling.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding node.helmet-missing at the affected project files. Treat this as a concrete finding. Problem: Express appears to be used, but Helmet was not detected. Required change: Install and apply Helmet or equivalent security headers early in the Express middleware stack. Existing recommendation: Install and use helmet() early in the middleware stack, or document equivalent security header handling. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `node.helmet-missing` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 6. It works, but your endpoints have no brakes.

- [ ] Fix this finding.
- Check ID: `node.rate-limit-missing`
- Severity: `medium`
- Category: `node`
- Location: `project-wide`
- Heuristic: `yes`
- Tags: node, api, availability
- Problem: An API server appears to exist, but no rate-limit dependency or middleware was detected.
- Recommendation: Add route-appropriate rate limiting, for example express-rate-limit or @fastify/rate-limit, especially for auth and write endpoints.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding node.rate-limit-missing at the affected project files. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: An API server appears to exist, but no rate-limit dependency or middleware was detected. Required change: Add appropriate rate limiting for API routes, especially authentication and write endpoints. Existing recommendation: Add route-appropriate rate limiting, for example express-rate-limit or @fastify/rate-limit, especially for auth and write endpoints. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `node.rate-limit-missing` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.


## Final Verification

- [ ] Run the relevant test suite.
- [ ] Run `itworksbut scan` again.
- [ ] Confirm there are no remaining findings at or above the configured fail-on severity.

